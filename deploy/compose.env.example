# deploy/.env for deploy/compose.yaml
HOST=server.local            # name or IP the browser uses for this box (also a SAN of the cert)
PANEL_KEY=                   # openssl rand -hex 32
BETTER_AUTH_SECRET=          # openssl rand -base64 32
CONTACT=admin@example.com
LOG_LEVEL=info
