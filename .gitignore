# Go
bin/
/dist/
*.exe
# Node / web
node_modules/
web/dist/
# Local instance data (never commit worlds or jars)
/data/
/servers/
*.jar
# The Gradle wrapper jar is part of the build, not a build product: CI and the release build the agent with it.
!agent/gradle/wrapper/gradle-wrapper.jar
# OS / editors
.DS_Store
.idea/
.vscode/
*.log
.env

# Beacon
data/
.env.local
.next/
next-env.d.ts
tsconfig.tsbuildinfo

# Landing (static export)
landing/dist/
landing/.astro/
